<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.MainActivity">

    <FrameLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/mainNavigationView"
        style="@style/navigationViewStyle.mainNavigation"
        android:visibility="visible"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/menu_bottom_bar" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/marsNavigationView"
        style="@style/navigationViewStyle.mainNavigation"
        android:visibility="gone"
        app:menu="@menu/menu_bottom_bar_mars" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        style="@style/fabStyle"
        app:layout_anchor="@id/bottomAppBar"
        tools:ignore="ContentDescription" />

    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/bottomAppBar"
        style="@style/bottomAppBarStyle"
        app:contentInsetStart="0dp" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/marsCamerasNavigationView"
        style="@style/navigationViewStyle"
        android:visibility="gone"
        app:menu="@menu/menu_mars_cameras" />

    <View
        android:id="@+id/viewToHideCamerasMenu"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/color_gray"
        android:visibility="gone" />

    <View
        style="@style/settingsFragmentLineViewStyle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <include layout="@layout/bottom_sheet_layout" />
</androidx.coordinatorlayout.widget.CoordinatorLayout>